<h2>Edit subject</h2>

<%= render "form.html", changeset: @changeset,
                        action: subject_path(@conn, :update, @subject) %>

<span><%= link "Back", to: subject_path(@conn, :index) %></span>
