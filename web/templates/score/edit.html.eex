<h2>編集</h2>

<%= render "form.html", changeset: @changeset,
  action: score_path(@conn, :update, @score),
  student: @student, subject: @subject %>

<span><%= link "戻る", to: score_path(@conn, :index) %></span>
