<h2>New score</h2>

<%= render "form.html", changeset: @changeset,
    action: score_path(@conn, :create),
    student: @student, subject: @subject %>

<span><%= link "Back", to: score_path(@conn, :index) %></span>
